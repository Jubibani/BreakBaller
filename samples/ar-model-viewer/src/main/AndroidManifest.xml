<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.ar.sceneform.samples.gltf">

    <!-- Always needed for AR. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Needed to load gltf from network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--Speech Recog-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--Haptic Feedback-->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Sceneform requires OpenGLES 3.0 or later. -->
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <!-- Indicates that this app requires Google Play Services for AR ("AR Required") and results in
         the app only being visible in the Google Play Store on devices that support ARCore.
         For an "AR Optional" app, remove this tag. -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".library.App"
        android:icon="@drawable/splashscreen"
        android:label="Augment-ED"
        android:theme="@style/AppTheme">

        <!-- Indicates that this app requires Google Play Services for AR ("AR Required") and causes
        the Google Play Store to download and intall Google Play Services for AR along with the app.
        For an "AR Optional" app, specify "optional" instead of "required". -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />


<!--Splash Activity-->
<activity
    android:name=".library.screens.SplashActivity"
    android:configChanges="orientation|screenSize"
    android:exported="true"
    android:screenOrientation="locked">
<!--    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>-->
</activity>

<!--Main Activity-->
<activity
    android:name=".library.screens.MainActivity"
    android:configChanges="orientation|screenSize"
    android:exported="true"
    android:screenOrientation="locked">

</activity>


        <!--Activity-->
        <activity
            android:name=".library.Activity"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:screenOrientation="locked">
 <!--           <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

        <!--component: SpeechRecognition-->
        <activity
            android:name=".library.components.SpeechRecognitionActivity"
            android:configChanges="orientation|screenSize">
            <!--android:exported="true">-->
            <!--<intent-filter>-->
            <!--    <action android:name="android.intent.action.MAIN" />-->
            <!--    <category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

        <!--component: SpeechRecognition (2)-->
        <activity
            android:name=".library.components.CameraSpeechActivity"
            android:exported="true">

        </activity>

        <!--Library Activity-->
        <activity
            android:name=".library.screens.LibraryActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Practice Activity-->
        <activity
            android:name=".library.screens.PracticeActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>


        <!--Models-->
        <activity
            android:name=".library.gallery.AmphibianActivity"
            android:exported="false"
            android:label="Bacteria Activity">
        </activity>
        <activity
            android:name=".library.gallery.BacteriaActivity"
            android:exported="false"
            android:label="Bacteria Activity">
        </activity>
        <activity
            android:name=".library.gallery.DigestiveActivity"
            android:exported="false"
            android:label="Bacteria Activity">
        </activity>
        <activity
            android:name=".library.gallery.HeartActivity"
            android:exported="false"
            android:label="Bacteria Activity">
        </activity>
        <activity
            android:name=".library.gallery.PlatypusActivity"
            android:exported="false"
            android:label="Bacteria Activity">
        </activity>


    </application>
</manifest>